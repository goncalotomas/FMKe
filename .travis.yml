dist: trusty
language: erlang
sudo: false
otp_release:
  - 20.2
services:
  - docker
install:
  - make all
script:
  - make lint
  - make ct
  - make bench
  - make test-multiple-releases
  - rebar3 as test coveralls send
cache:
  directories:
  - "$HOME/.cache/rebar3/hex/default"
