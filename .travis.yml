language: erlang
sudo: false
otp_release:
  - 18.3.4
  - 19.3
services:
  - docker
install:
  - make all
before_script:
  - kerl list installations
script:
  - ./travis.sh test fmke
  - ./travis.sh test antidote
  - ./travis.sh bench antidote
cache:
  directories:
  - "$HOME/.cache/rebar3/hex/default"
sudo: required
dist: trusty
